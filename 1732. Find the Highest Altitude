class Solution {
    public int largestAltitude(int[] gain) {
        int max=Integer.MIN_VALUE;
        int alti=0;
        for(int i=0;i<gain.length;i++)
        {
            alti+=gain[i];
            if(alti > max)
                max=alti;
            
        }
        if(max < 0)
            return 0;
        else
            return max;
    }
}
